import { utilStyles } from "@/styles";
import { View } from "react-native";

export const TrackSeparator = () => (
  <View style={{ ...utilStyles.listItemSeparator, marginVertical: 9, marginLeft: 60 }} />
);
